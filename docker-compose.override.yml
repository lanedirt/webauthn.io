# Development override for docker-compose.yml
# This file mounts the source code for live development

services:
  django:
    volumes:
      - ./_app:/usr/src/app
      - static_files_volume:/usr/src/app/static
    environment:
      - DEBUG=true